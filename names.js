function full_name(first_name, last_name) {
    let person = {
        first_name: first_name,
        last_name: last_name
    }
    return person
}

/* console.log(full_name("giovanni", "lumini")); */

module.exports = full_name;